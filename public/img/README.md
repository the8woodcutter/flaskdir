## THIS IS THE `/img` DIRECTORY
### Please note:

* The purpose of this directory is literally *unknown* yet
* This directory was put here in case it gets used later
	- The creator of `flaskdir` uses a `/img` directory on other projects